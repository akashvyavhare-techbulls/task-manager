name: compile-ci 

on:
  pull_request:

jobs:
  complie:
    runs-on: ubuntu-latest

    steps:
      - name: git checkout
        uses: actions/checkout@v4

      - name: setup Java
        uses: actions/setup-java@v4

      - name: maven verify 
        run: mvn verify
